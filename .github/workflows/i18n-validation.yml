name: i18n validation

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  smoke-test:
    name: CI smoke test
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Echo
        run: |
          echo "Hello from repo-root workflow"
          ls -la
          echo "---"
          ls MarineSABRES_SES_Shiny/ | head -5
