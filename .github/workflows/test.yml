name: R Tests

# Repo-root CI for the testthat unit suite. Closes #7: the comprehensive matrix
# workflow under MarineSABRES_SES_Shiny/.github/workflows/test.yml never ran
# because GitHub Actions only reads workflows from the repository root. This is a
# lean single-target version (ubuntu + R 4.4, no E2E/coverage) chosen to keep the
# R dependency install (~40 CRAN packages) tractable; expand the matrix later if
# CI time allows.

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  unit-tests:
    name: testthat unit suite
    runs-on: ubuntu-22.04   # matches the posit jammy binary repo used below
    defaults:
      run:
        working-directory: MarineSABRES_SES_Shiny
    env:
      R_KEEP_PKG_SOURCE: "no"
    steps:
      - uses: actions/checkout@v4

      - name: Setup R
        uses: r-lib/actions/setup-r@v2
        with:
          r-version: '4.4'
          use-public-rspm: true

      # Pin an explicit, known user-library path (don't rely on setup-r exporting
      # R_LIBS_USER — it may be empty, which would make the cache a no-op). All
      # later steps inherit this via $GITHUB_ENV; R adds it to .libPaths at startup.
      - name: Configure R library path
        run: |
          echo "R_LIBS_USER=${{ github.workspace }}/.rlib" >> "$GITHUB_ENV"
          mkdir -p "${{ github.workspace }}/.rlib"
        shell: bash

      # Cache that library so the ~45-package install is skipped on repeat runs.
      # Key on the files that define the install set; restore-keys seeds from an
      # older cache when the key changes.
      - name: Cache R packages
        uses: actions/cache@v4
        with:
          path: ${{ github.workspace }}/.rlib
          key: R-${{ runner.os }}-4.4-${{ hashFiles('MarineSABRES_SES_Shiny/deployment/required_packages.R', '.github/workflows/test.yml') }}
          restore-keys: |
            R-${{ runner.os }}-4.4-

      - name: Setup Pandoc
        uses: r-lib/actions/setup-pandoc@v2

      - name: Setup Chrome (for shinytest2 E2E)
        id: setup-chrome
        uses: browser-actions/setup-chrome@v1
        with:
          chrome-version: stable

      - name: Install system dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y libcurl4-openssl-dev libssl-dev libxml2-dev \
            libfontconfig1-dev libharfbuzz-dev libfribidi-dev \
            libfreetype6-dev libpng-dev libtiff5-dev libjpeg-dev \
            libglpk-dev libgmp-dev
        shell: bash

      - name: Install R dependencies
        run: |
          # Use the project's canonical package list — it parses global.R's
          # library() calls (so it never drifts) plus a fixed augment set, and
          # excludes the optional torch. Posit binary repo => fast, no compiling.
          options(repos = c(CRAN = "https://packagemanager.posit.co/cran/__linux__/jammy/latest"))
          # Ensure the cached user library is present and first on the search path,
          # so install.packages writes there (and a cache hit is seen as installed).
          lib <- Sys.getenv("R_LIBS_USER")
          if (nzchar(lib)) { dir.create(lib, recursive = TRUE, showWarnings = FALSE); .libPaths(c(lib, .libPaths())) }
          source("deployment/required_packages.R")  # defines REQUIRED_PACKAGES
          # officer + flextable: Word-export deps exercised by the download-handler
          # hardening tests (forced-writer-failure paths). Without them those tests
          # get a "package not available" error instead of the expected one.
          want <- c(REQUIRED_PACKAGES, "testthat", "officer", "flextable",
                    "shinytest2", "chromote")   # E2E browser-driver tests
          # Install only what the cache didn't already provide (install.packages
          # pulls recursive deps; all land in the cached lib).
          need <- setdiff(want, rownames(installed.packages()))
          cat(sprintf("Cache provided %d/%d; installing %d: %s\n",
                      length(want) - length(need), length(want), length(need),
                      paste(need, collapse = ", ")))
          if (length(need)) install.packages(need)
          missing <- setdiff(want, rownames(installed.packages()))
          if (length(missing)) stop("Failed to install: ", paste(missing, collapse = ", "))
        shell: Rscript {0}

      - name: Prepare test fixtures
        run: |
          dir.create("data", showWarnings = FALSE)
          fx <- list.files("tests/fixtures/templates", pattern = "\\.json$", full.names = TRUE)
          if (length(fx) > 0) file.copy(fx, "data", overwrite = TRUE)
          message(sprintf("Fixtures copied: %d", length(fx)))
        shell: Rscript {0}

      - name: Run testthat suite (per-file process isolation)
        run: |
          set -uo pipefail
          # Run each test file in its OWN R process via tests/ci_run_file.R
          # (per-file isolation removes cross-file global-state leakage). shinytest2
          # suites (incl. the full-app AppDriver E2E) run against the Chrome
          # installed above (CHROMOTE_CHROME). No exclusions — the full suite runs.
          exclude='^$'   # matches nothing
          ran=0; failed=0; failed_files=""
          for f in tests/testthat/test-*.R; do
            b=$(basename "$f")
            if echo "$b" | grep -qE "$exclude"; then continue; fi
            ran=$((ran + 1))
            if ! Rscript tests/ci_run_file.R "$f"; then
              failed=$((failed + 1)); failed_files="$failed_files $b"
            fi
          done
          echo ""
          echo "Ran $ran files in isolated R processes (full suite, no exclusions); failed: $failed"
          if [ "$failed" -ne 0 ]; then echo "FAILED FILES:$failed_files"; exit 1; fi
          echo "All test files passed."
        shell: bash
        env:
          CI: "true"
          CHROMOTE_CHROME: ${{ steps.setup-chrome.outputs.chrome-path }}
